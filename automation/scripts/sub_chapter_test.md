# Sub Chapter Test

This file tests nested folder → sub-chapter mapping for DXC-166.

## Purpose

Verify that a file in `automation/scripts/` creates a sub-chapter under the "automation" chapter in the Playbook.
