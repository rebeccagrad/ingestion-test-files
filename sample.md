# Sample Markdown Document

This is a **markdown file** that should be converted to RTE HTML — _not_ wrapped in a CodeBlock.

## Features Tested

- Code block ingestion for recognized extensions
- Root directory filtering
- Taxonomy tag inheritance
- Taxonomy change detection and re-sync

## Code Example

```java
System.out.println("This is inline code in markdown");
```

> This blockquote should be converted to proper RTE markup.

---
_Test commit for ingestion recovery retest - 2026-03-02 (post-failure recovery)_
