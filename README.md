# Ingestion Test Files

This repository contains sample files for testing content ingestion from GitHub.

## Structure

- Root level: general test files
- `src/`: subdirectory for testing root directory filtering

## File Types

- `.md` — Markdown files (should be converted to rich text)
- `.py` — Python files (should be ingested as code blocks)
